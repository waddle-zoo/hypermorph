# requirements/archive

Historical requirement fixtures that are deliberately outside the manual
`scripts/gh-to-gastown-sync.sh` intake path. Archive conservatively instead of
deleting: these files remain evidence of what was exercised, but editing them
must not create or update a Gas Town bead.
